#include "main.h"

/**
 * _atoi - extracts numbers from string
 * @s: string
 *
 * Return: extracted number
 */
int _isdigit(char *c)
{
	if (*c >= '0' && *c <= '9')
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
int _atoi(char *s)
{
	
}
