#include "main.h"
/**
 * _abs - calculates the absolute value of a number
 * @n: number tobe calculated
 *
 * Return: the absolute value of a nuber given
 */
int _abs(int n)
{
	if (n < 0)
	{
		n = (-1) * n;
	}
	return (n);
}
